$kind: http-request
url: "{{baseUrl}}/api/notes/{{noteId}}"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('single note is returned', function () {
        pm.response.to.have.status(200);
        pm.expect(pm.response.json().data.id).to.eql(pm.collectionVariables.get('noteId'));
      });
    language: text/javascript
order: 4000
